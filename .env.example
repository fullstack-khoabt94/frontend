# Base URL of the REST API. Leave as /api to proxy through Vite in dev.
VITE_API_BASE_URL=/api

# While true the app talks to the in-browser mock backend
# (src/lib/api/mock-adapter.ts) instead of the network.
# Set to false once the real backend is running.
VITE_USE_MOCK_API=true
