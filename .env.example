# Base URL the frontend calls. Keep it as /api so requests stay same-origin and
# the Vite dev server proxies them to the backend (see vite.config.ts).
VITE_API_BASE_URL=/api

# Where the dev proxy forwards /api to. Defaults to the Spring Boot dev port.
VITE_API_PROXY_TARGET=http://localhost:8080
